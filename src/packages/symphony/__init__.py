"""Symphony: orchestrates headless coding-agent runs against tracker-driven work.

This package is built bottom-up toward Core Conformance (SPEC §18.1). At this
milestone (M0) only the tooling skeleton, typed package layout, and the
:mod:`symphony.exceptions` hierarchy are in place; behavior lands in later PRs.
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
